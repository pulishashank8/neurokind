{
  "name": "neurokid",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["web"],
  "scripts": {
    "postinstall": "npm install --prefix web",
    "build": "npm install --prefix web && cd web && npx prisma generate",
    "start": "npm run prod --prefix web",
    "dev": "npm run dev --prefix web"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
