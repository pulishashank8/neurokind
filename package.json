{
  "name": "neurokid",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd web && npm install",
    "build": "cd web && npm install && npx prisma generate",
    "start": "cd web && npm run prod",
    "dev": "cd web && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
