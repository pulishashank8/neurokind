# NeuroKind Environment Variables

# =============================================================================
# REQUIRED VARIABLES (App will not start without these)
# =============================================================================

# PostgreSQL Database URL
# Format: postgresql://user:password@host:port/database
# Local: postgresql://postgres:postgres@localhost:5432/neurokind
# Supabase: Get from Supabase project settings > Database > Connection String
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/neurokind"

# NextAuth Secret (Generate with: openssl rand -base64 32)
# MUST be at least 32 characters
NEXTAUTH_SECRET="your-super-secret-key-min-32-chars-change-this-in-production"

# NextAuth URL (Your app's public URL)
# Local: http://localhost:3000
# Production: https://your-domain.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (for "Continue with Google" login)
# If not set: Google login button won't appear
# Get from: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
# 3. For production, add: https://your-domain.com/api/auth/callback/google
# GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"

# =============================================================================
# OPTIONAL VARIABLES (Features degrade gracefully if missing)
# =============================================================================

# Redis URL (for caching and rate limiting)
# If not set: Uses in-memory rate limiting (works but resets on restart)
# Local: redis://localhost:6379
# Upstash: redis://default:password@host:port
# REDIS_URL="redis://localhost:6379"

# OpenAI API Key (for AI Support features - DEPRECATED, use GROQ_API_KEY)
# If not set: AI Support page shows placeholder message
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY="sk-..."

# Groq API Key (for AI Chat features - FREE tier available!)
# If not set: AI chat will not be available
# Get free key from: https://console.groq.com/keys
# Uses fast Llama 3.3 70B model with generous free tier
# GROQ_API_KEY="gsk_..."

# Google Places API Key (for Provider location search)
# If not set: Provider search shows sample data
# Get from: https://console.cloud.google.com/apis/credentials
# GOOGLE_PLACES_API_KEY="AIza..."

# Formspree Endpoint (for contact/support form)
# If not set: Contact form will be disabled with message
# Get from: https://formspree.io/forms
# NEXT_PUBLIC_FORMSPREE_ENDPOINT="https://formspree.io/f/your-form-id"

# =============================================================================
# PUBLIC VARIABLES (Exposed to client-side)
# =============================================================================

# Public App URL (defaults to NEXTAUTH_URL if not set)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node Environment (set automatically by deployment platform)
NODE_ENV="development"

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================
#
# For Vercel + Supabase:
# 1. Create Supabase project at https://supabase.com
# 2. Get DATABASE_URL from Settings > Database > Connection String (use 'Session pooler' mode)
# 3. Add all required env vars to Vercel project settings
# 4. Optional: Add Upstash Redis for production rate limiting
#
# For local development:
# 1. Run Docker: docker compose up -d (from project root)
# 2. Run migrations: npm run db:push
# 3. Seed database: npm run db:seed
# 4. Start dev server: npm run dev
#
